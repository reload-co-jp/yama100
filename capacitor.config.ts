import type { CapacitorConfig } from "@capacitor/cli"

const config: CapacitorConfig = {
  appId: "com.reload.yama100",
  appName: "Yama100",
  webDir: "out",
  server: {
    androidScheme: "https",
  },
}

export default config
